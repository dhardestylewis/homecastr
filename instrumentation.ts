// Server-side instrumentation (no-op after dd-trace removal)
// Kept as a placeholder for future observability (e.g. Cloudflare Workers Analytics)
export async function register() {
  // dd-trace removed during Vercel → Cloudflare migration
}
